; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html
[env:upesy_wroom]
platform = espressif32
board = upesy_wroom
framework = arduino
monitor_speed = 115200

; Versions pinned to the known-good build (see README). FirebaseClient (mobizt)
; changes its API across releases, so keep it pinned and bump deliberately.
lib_deps =
    adafruit/DHT sensor library@1.4.7
    adafruit/Adafruit Unified Sensor@1.1.15
    claws/BH1750@1.3.0
    mobizt/FirebaseClient@2.2.10

; For a fully reproducible build you can also pin the platform, e.g.:
;   platform = espressif32@<version>
; (left unpinned to use your currently-installed core).

monitor_filters = 
    direct
    time
    send_on_enter